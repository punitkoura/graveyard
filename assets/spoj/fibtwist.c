#include<stdio.h>

int main()
{
    long long int t,m,n;
    scanf("%lld",&t);

    int *arr = (int*)malloc(sizeof(int)*1000000000);

    while(t--)
    {
        arr
        scanf("%lld",n);
        scanf("%lld",m);
    }
}
